---
phase: 07-polish-and-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/bottom-tab-bar.tsx
  - src/app/(main)/layout.tsx
  - src/components/ui/button.tsx
  - src/components/watering/dashboard-plant-card.tsx
  - src/components/plants/plant-card.tsx
  - src/components/plants/filter-chips.tsx
  - src/components/rooms/room-card.tsx
  - src/app/(main)/dashboard/page.tsx
  - src/app/(main)/rooms/page.tsx
  - src/app/(main)/rooms/[id]/page.tsx
autonomous: true
requirements:
  - UIAX-01
user_setup: []

must_haves:
  truths:
    - "Bottom tab bar visible on mobile (<640px) with Dashboard, Plants, Rooms, Notifications tabs"
    - "Top nav Plants/Rooms links hidden on mobile, visible on desktop"
    - "All interactive elements meet 44x44px minimum touch target on mobile"
    - "Card grids reflow to 1 column on mobile, 2 on sm, 3 on lg"
    - "Main content has bottom padding on mobile to clear the fixed tab bar"
    - "Skip-to-content link is the first tabbable element in the layout"
  artifacts:
    - path: "src/components/layout/bottom-tab-bar.tsx"
      provides: "Mobile-only fixed bottom navigation bar"
      min_lines: 40
    - path: "src/app/(main)/layout.tsx"
      provides: "Updated layout with skip link, bottom tab bar, responsive nav, main padding"
      contains: "BottomTabBar"
  key_links:
    - from: "src/app/(main)/layout.tsx"
      to: "src/components/layout/bottom-tab-bar.tsx"
      via: "import and render after main"
      pattern: "BottomTabBar"
    - from: "src/components/layout/bottom-tab-bar.tsx"
      to: "usePathname"
      via: "active tab detection"
      pattern: "pathname.startsWith"
---

<objective>
Mobile navigation infrastructure, responsive card grids, and systematic touch target compliance across the app.

Purpose: Delivers the mobile-first layout (D-01, D-02, D-03) so the app is fully usable on phones with thumb-friendly navigation and properly reflowing content grids.
Output: BottomTabBar component, updated main layout with skip link and responsive nav, touch-target-compliant buttons/pills, responsive card grids.
</objective>

<execution_context>
@c:/Dev/poc/plantz-gsd/.claude/get-shit-done/workflows/execute-plan.md
@c:/Dev/poc/plantz-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-and-accessibility/07-CONTEXT.md
@.planning/phases/07-polish-and-accessibility/07-RESEARCH.md
@.planning/phases/07-polish-and-accessibility/07-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/app/(main)/layout.tsx:
- Server Component layout wrapping all authenticated pages
- Currently has: <header> with <nav>, top nav links (Plants, Rooms), NotificationBell, UserMenu
- <main> currently has `className="mx-auto max-w-5xl px-4 py-6"`
- NotificationBell receives `count` and `items` props

From src/components/ui/button.tsx:
- buttonVariants cva with sizes: default (h-8), xs (h-6), sm (h-7), lg (h-9), icon (size-8), icon-xs (size-6), icon-sm (size-7), icon-lg (size-9)
- Button wraps Base UI ButtonPrimitive

From src/components/plants/filter-chips.tsx:
- `export function FilterChips(...)` — renders pill-style filter buttons
- Buttons use `h-7` (28px) sizing

From src/components/watering/dashboard-plant-card.tsx:
- InlineSnoozePills uses `<button>` with `px-3 py-1.5 text-xs` (est. ~28px height)

From src/components/auth/user-menu.tsx:
- DropdownMenuTrigger renders an avatar circle, currently using button layout
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BottomTabBar and update main layout with mobile navigation and skip link</name>
  <files>
    src/components/layout/bottom-tab-bar.tsx,
    src/app/(main)/layout.tsx
  </files>
  <read_first>
    src/app/(main)/layout.tsx,
    src/components/reminders/notification-bell.tsx,
    src/components/auth/user-menu.tsx,
    .planning/phases/07-polish-and-accessibility/07-UI-SPEC.md
  </read_first>
  <action>
**1. Create `src/components/layout/bottom-tab-bar.tsx`** (Client Component):

```tsx
"use client";
```

- Import `Link` from `next/link`, `usePathname` from `next/navigation`, icons `LayoutDashboard`, `Leaf`, `DoorOpen`, `Bell` from `lucide-react`, `cn` from `@/lib/utils`.
- Define `TABS` array with 4 entries:
  - `{ href: "/dashboard", icon: LayoutDashboard, label: "Dashboard" }`
  - `{ href: "/plants", icon: Leaf, label: "Plants" }`
  - `{ href: "/rooms", icon: DoorOpen, label: "Rooms" }`
  - `{ href: "/notifications", icon: Bell, label: "Alerts" }` — this 4th tab is a link to `/dashboard` with a notification badge overlay; see below.
- Actually, per D-01, the Notification bell moves to the bottom tab bar on mobile. Since NotificationBell is a dropdown, the 4th tab should receive `reminderCount` as a prop and show a red dot badge when count > 0. On tap, it should open the notification dropdown. However, this is complex — simpler approach per UI-SPEC: the 4th tab links to `/dashboard` (where notifications appear). Accept a `notificationCount` prop.
- Component signature: `export function BottomTabBar({ notificationCount }: { notificationCount: number })`
- Render a `<nav aria-label="Main navigation" className="fixed bottom-0 left-0 right-0 z-40 border-t border-border bg-background pb-[env(safe-area-inset-bottom)] sm:hidden">`.
- Inside: `<div className="flex h-14 items-stretch">`.
- Map over tabs. For each: `<Link key={href} href={href} aria-current={isActive ? "page" : undefined} className={cn("flex flex-1 flex-col items-center justify-center gap-0.5 text-xs min-h-[44px]", isActive ? "text-accent" : "text-muted-foreground")}>`.
- Icon: `<Icon className="h-5 w-5" aria-hidden="true" />`. For the Bell tab, wrap in a `relative` div and add a badge dot: `{notificationCount > 0 && <span className="absolute -top-1 -right-1 flex h-4 w-4 items-center justify-center rounded-full bg-destructive text-[10px] font-bold text-white">{notificationCount > 9 ? "9+" : notificationCount}</span>}`.
- Label: `<span>{label}</span>`.
- Active detection: `pathname === href || pathname.startsWith(href + "/")` for all except dashboard which should be exact match `pathname === "/dashboard"`.

**2. Update `src/app/(main)/layout.tsx`**:

- Import `BottomTabBar` from `@/components/layout/bottom-tab-bar`.
- Add skip-to-content link as FIRST child inside the outer `<div className="min-h-screen bg-background">`, BEFORE the demo banner and header:
  ```tsx
  <a
    href="#main-content"
    className="sr-only focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-50 focus:rounded-md focus:bg-background focus:px-4 focus:py-2 focus:text-sm focus:font-medium focus:shadow-md focus:ring-2 focus:ring-ring"
  >
    Skip to content
  </a>
  ```
- Add `aria-label="Top navigation"` to the existing `<nav>` inside `<header>`.
- Hide Plants and Rooms links on mobile: change the `<div className="flex items-center gap-4">` containing Plants/Rooms links to `<div className="hidden items-center gap-4 sm:flex">`.
- Hide NotificationBell on mobile: wrap the NotificationBell in the top nav's right-side div with `<div className="hidden sm:block">` around it. The bell functionality moves to the bottom tab bar on mobile.
- Update `<main>` element: add `id="main-content"` and change className to `"mx-auto max-w-5xl px-4 py-6 pb-20 sm:pb-6"` (pb-20 = 80px to clear 56px tab bar + safe area; sm:pb-6 restores desktop padding).
- Add `<BottomTabBar notificationCount={reminderCount} />` AFTER `</main>`, still inside the outer div. This will be rendered by the server component and passed the already-computed `reminderCount`.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/bottom-tab-bar.tsx` exists and contains `"use client"`, `aria-label="Main navigation"`, `sm:hidden`, `LayoutDashboard`, `Leaf`, `DoorOpen`, `Bell`, `min-h-[44px]`, `pb-[env(safe-area-inset-bottom)]`
    - `src/app/(main)/layout.tsx` contains `Skip to content`, `id="main-content"`, `pb-20 sm:pb-6`, `BottomTabBar`, `aria-label="Top navigation"`, `hidden items-center gap-4 sm:flex` (for Plants/Rooms links), `hidden sm:block` (around NotificationBell)
    - BottomTabBar has 4 tabs: Dashboard, Plants, Rooms, and Alerts/Notifications
    - The skip link uses `sr-only focus:not-sr-only` pattern
    - `href="#main-content"` in skip link matches `id="main-content"` on main element
  </acceptance_criteria>
  <done>Bottom tab bar renders on mobile with 4 thumb-friendly tabs. Top nav links hidden on mobile. Skip-to-content link is first tabbable element. Main content has bottom padding to clear fixed tab bar.</done>
</task>

<task type="auto">
  <name>Task 2: Touch target audit and responsive card grid reflow</name>
  <files>
    src/components/ui/button.tsx,
    src/components/watering/dashboard-plant-card.tsx,
    src/components/plants/plant-card.tsx,
    src/components/plants/filter-chips.tsx,
    src/components/rooms/room-card.tsx,
    src/app/(main)/dashboard/page.tsx,
    src/app/(main)/rooms/page.tsx,
    src/app/(main)/rooms/[id]/page.tsx
  </files>
  <read_first>
    src/components/ui/button.tsx,
    src/components/watering/dashboard-plant-card.tsx,
    src/components/plants/plant-card.tsx,
    src/components/plants/filter-chips.tsx,
    src/components/rooms/room-card.tsx,
    src/app/(main)/dashboard/page.tsx,
    src/app/(main)/rooms/page.tsx,
    src/app/(main)/rooms/[id]/page.tsx,
    src/components/watering/dashboard-client.tsx,
    src/components/plants/plant-grid.tsx,
    .planning/phases/07-polish-and-accessibility/07-UI-SPEC.md
  </read_first>
  <action>
**Touch Target Fixes (D-03 — all interactive elements must meet 44x44px minimum):**

Per UI-SPEC, use negative-margin + padding expansion trick to increase hit area without affecting visual layout.

1. **`src/components/ui/button.tsx`** — Add a touch-target utility size. In the `buttonVariants` cva, do NOT change existing sizes (they affect desktop too). Instead, the approach is to have consumers add mobile touch expansion classes. However, for the `icon` and `icon-sm` sizes which are ALWAYS too small, add minimum sizing:
   - `icon` size: change from `"size-8"` to `"size-8 min-h-[44px] min-w-[44px]"` — this expands the hit area on all devices. The visual icon stays at 32px via the inner SVG sizing, but the tappable area is 44px.
   - Actually, the cleaner approach per UI-SPEC: use the negative margin trick on specific consumers rather than changing the base component. Do NOT modify button.tsx globally. Instead, fix touch targets at the consumer level.

   **Decision: Leave button.tsx unchanged.** Fix touch targets in consumer components below.

2. **`src/components/watering/dashboard-plant-card.tsx`**:
   - InlineSnoozePills: Each `<button>` has `px-3 py-1.5 text-xs`. Add `min-h-[44px]` to ensure touch target compliance: change className to `"rounded-md px-3 py-1.5 text-xs font-medium text-muted-foreground bg-muted/60 hover:bg-muted hover:text-foreground transition-colors disabled:opacity-50 min-h-[44px]"`.
   - The snooze pills container already has gap-2. The min-h expansion is sufficient.

3. **`src/components/plants/filter-chips.tsx`**:
   - Read the full file first to find the exact button/pill rendering.
   - Each filter chip pill button: add `min-h-[44px]` to the className to expand tap area.
   - If chips use Button component with size="sm" (h-7 = 28px), add `min-h-[44px]` to each chip's className.

4. **`src/components/plants/plant-card.tsx`** — The card link is already large enough (full card is the tap target). No change needed.

5. **`src/components/rooms/room-card.tsx`** — Read the file. If it has small action buttons (edit, delete), add `min-h-[44px] min-w-[44px]` to those button classNames.

6. **Back-to-catalog link** in `src/app/(main)/plants/[id]/page.tsx` — The `<Link href="/plants">` with `<ChevronLeft>` text link is ~20px. Add `p-2 -m-2` to expand hit area: change className to `"flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground transition-colors p-2 -m-2"`. (This will be done in Plan 03 Task 1 when h1 tabIndex is added to this file — note for Plan 03.)

**Responsive Card Grid Reflow (D-02):**

7. **`src/app/(main)/dashboard/page.tsx`** — Find the `DashboardSkeleton` grid: change `"grid grid-cols-1 md:grid-cols-2 gap-4"` to `"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4"`.
   - Also check `dashboard-client.tsx` for the actual plant card grid classes and update similarly.

8. **`src/app/(main)/rooms/page.tsx`** — The room card grid currently uses `"grid grid-cols-1 gap-4 sm:grid-cols-2"`. Update to `"grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3"` to add 3-col on desktop.

9. **`src/app/(main)/rooms/[id]/page.tsx`** — Read the file. If it has a plant grid, update similarly to 1-col mobile, 2-col sm, 3-col lg.

10. **Dashboard urgency section grids** — Read `src/components/watering/dashboard-client.tsx` to find where DashboardPlantCard is rendered in a grid. Update grid classes to `"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4"`.

11. **PlantGrid component** — Read `src/components/plants/plant-grid.tsx`. Update its grid classes to `"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4"`.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/watering/dashboard-plant-card.tsx` InlineSnoozePills buttons contain `min-h-[44px]`
    - `src/components/plants/filter-chips.tsx` pill buttons contain `min-h-[44px]`
    - Dashboard skeleton grid uses `sm:grid-cols-2 lg:grid-cols-3`
    - `src/app/(main)/rooms/page.tsx` grid uses `sm:grid-cols-2 lg:grid-cols-3`
    - `src/components/plants/plant-grid.tsx` (or equivalent) grid uses `sm:grid-cols-2 lg:grid-cols-3`
    - Dashboard card grid (in dashboard-client.tsx) uses `sm:grid-cols-2 lg:grid-cols-3`
    - All existing tests pass (`npx vitest run` exits 0)
  </acceptance_criteria>
  <done>All snooze pills and filter chips meet 44px touch target. Card grids reflow 1-col on mobile, 2-col on sm (640px+), 3-col on lg (1024px+). Dashboard, plants, and rooms grids all follow the same responsive pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side navigation | Bottom tab bar uses client-side routing; no untrusted input |
| Skip link anchor | `href="#main-content"` is a same-page anchor; no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | BottomTabBar | accept | Component renders static links to known internal routes; no user input in href values |
| T-07-02 | Tampering | Skip link | accept | Static anchor tag with hardcoded href; no dynamic content |
| T-07-03 | Info Disclosure | NotificationBell count on tab bar | accept | Count is already computed server-side and passed to existing NotificationBell; no new data exposure |
</threat_model>

<verification>
- `npm run build` completes without errors
- `npx vitest run` passes all existing tests
- Visual check: On a 375px viewport, bottom tab bar is visible with 4 tabs; top nav Plants/Rooms links are hidden
- Visual check: On a 1024px+ viewport, bottom tab bar is hidden; top nav links are visible
- Tab key from page start focuses the skip link first
</verification>

<success_criteria>
- Bottom tab bar shows on mobile with Dashboard, Plants, Rooms, Notifications tabs at 44px+ touch targets
- Top nav adapts: Plants/Rooms links hidden on mobile, notification bell hidden on mobile (moved to tab bar)
- Skip-to-content link is keyboard accessible and jumps to main content
- All card grids reflow: 1-col < 640px, 2-col >= 640px, 3-col >= 1024px
- Touch targets on snooze pills and filter chips meet 44px minimum
- No layout breakage on desktop
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-accessibility/07-01-SUMMARY.md`
</output>

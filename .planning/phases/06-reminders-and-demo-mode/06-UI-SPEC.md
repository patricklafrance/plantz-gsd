---
phase: 6
slug: reminders-and-demo-mode
status: draft
shadcn_initialized: true
preset: base-nova / neutral / cssVariables
created: 2026-04-15
---

# Phase 6 — UI Design Contract

> Visual and interaction contract for Phase 6: Reminders and Demo Mode.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | components.json |
| Style | base-nova | components.json |
| Base color | neutral | components.json |
| Component library | base-ui (via shadcn base-nova) | components.json |
| Icon library | lucide-react | components.json |
| Font | Geist (sans) | globals.css `--font-sans` |
| CSS variables | enabled | components.json |
| Tailwind version | v4 (CSS-first `@theme`) | globals.css |

No new npm packages required for this phase. All UI primitives (DropdownMenu, Badge, Button, Switch, Dialog, AlertDialog, Calendar, Popover) are already installed.

---

## Spacing Scale

Declared values from `globals.css` — do not override:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge offsets, inline padding |
| sm | 8px | Compact element spacing, dropdown item vertical padding |
| md | 16px | Default element spacing, nav horizontal padding, page padding |
| lg | 24px | Section padding, page vertical padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

**Exceptions for this phase:**

| Element | Value | Reason |
|---------|-------|--------|
| Bell icon touch target | 44px min (use `p-2.5` on the button wrapper) | WCAG 2.5.5 touch target minimum for nav icon buttons |
| Snooze pill buttons | h-7 (sm size via Button `size="sm"`) | Pill row fits inline on plant detail without overwhelming layout |
| Notification badge position | `-top-1.5 -right-1.5` (6px offset) | Absolute overlay on Bell icon — consistent with established badge-on-icon pattern |

---

## Typography

Inherited from established project typography. Do not introduce new sizes or weights beyond this set:

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px (text-base) | 400 (normal) | 1.5 | General content, dropdown item secondary text |
| Label / UI small | 14px (text-sm) | 500 (medium) | 1.4 | Nav links, dropdown item names, preferences labels, snooze pill labels |
| Caption / Meta | 12px (text-xs) | 400 (normal) | 1.4 | Notification item room + status line, badge count, helper text |
| Heading | 20px (text-xl) | 600 (semibold) | 1.2 | Preferences page section headings |

**Weights in use:** 400 (normal) and 600 (semibold) only. Weight 500 (medium) is used for interactive labels only (nav links, form labels) — no new weights introduced.

---

## Color

All values from `globals.css` — light mode canonical references:

| Role | OKLCH Value | CSS Token | Usage |
|------|-------------|-----------|-------|
| Dominant (60%) | oklch(97% 0.004 286) | `--color-background` | Page background, dropdown panel background, preferences page background |
| Secondary (30%) | oklch(94% 0.006 286) | `--color-surface` | Card surfaces (preferences sections), dropdown item hover state |
| Accent (10%) | oklch(62% 0.10 155) | `--color-accent` | See reserved list below |
| Destructive | oklch(55% 0.22 27) | `--color-destructive` | Delete account action only |
| Muted | oklch(60% 0.006 286) | `--color-muted` | Bell icon resting state, notification meta text, empty state copy |
| Border | oklch(88% 0.006 286) | `--color-border` | Nav bottom border, card borders, input borders |

**Accent reserved for (exhaustive list):**
- Notification badge background (count > 0 state)
- Active/enabled state of the global reminders Switch thumb
- Active/enabled state of per-plant reminder Switch thumb
- Focus ring on bell icon button
- "Try demo" CTA link on the login page (text-accent)

Accent is NOT used for: snooze pills (use outline variant), nav links (use muted-foreground/foreground), preferences form labels, or the bell icon in its resting state.

**Demo mode banner:** Use `bg-surface border border-border` with a `text-muted-foreground` label. No accent — the demo state should be visually calm, not alarming.

**Snoozed plant indicator:** In the notification dropdown, snoozed plants are excluded entirely (filtered server-side). No special color treatment needed for snoozed state in the dropdown.

---

## Component Inventory

Components used in this phase — all already installed:

| Component | shadcn Path | Usage |
|-----------|-------------|-------|
| DropdownMenu | `@/components/ui/dropdown-menu` | Notification bell dropdown panel |
| Badge | `@/components/ui/badge` | Bell icon notification count overlay |
| Button | `@/components/ui/button` | Snooze pills (size="sm", variant="outline"), preferences Save buttons |
| Switch | Not yet installed — ADD via `npx shadcn@latest add switch` | Global reminders toggle, per-plant reminder toggle |
| Dialog | `@/components/ui/dialog` | Snooze "Custom" duration picker |
| AlertDialog | `@/components/ui/alert-dialog` | Delete account confirmation |
| Popover + Calendar | `@/components/ui/popover`, `@/components/ui/calendar` | Snooze custom date picker inside Dialog |
| Card | `@/components/ui/card` | Preferences page sections |
| Input | `@/components/ui/input` | Account settings forms (email, password) |
| Sonner (toast) | `@/components/ui/sonner` | Snooze confirmation, settings saved, account changes |
| Form | `@/components/ui/form` | Preferences account settings forms |
| Separator | `@/components/ui/separator` | Between preferences page sections |

**Switch component:** Not yet in `src/components/ui/`. Must be added before implementation: `npx shadcn@latest add switch`

---

## Interaction Contracts

### Notification Bell (Nav)

- **Resting state (count = 0):** Bell icon at `h-5 w-5 text-muted-foreground`. No badge rendered. Bell icon button has `aria-label="No plants need attention"`.
- **Active state (count > 0):** Bell icon at `h-5 w-5 text-foreground`. Badge overlaid at `-top-1.5 -right-1.5` with `bg-accent text-accent-foreground`. Count capped display at "99+". `aria-label="{count} plants need attention"`.
- **Bell animation:** No animation for v1. Static icon only.
- **Dropdown trigger:** Ghost button wrapper, `size="icon"` (32px), with extra padding for 44px touch target (`p-2.5` on the outer wrapper or use `size="icon-lg"` at 36px — use 36px icon-lg for the touch target). Positioned in the right action group of the nav, between nav links and logout.
- **Dropdown panel:** `w-72`, `align="end"`. Max 5 items visible before scroll (`max-h-[320px] overflow-y-auto`). Items listed in urgency order: overdue first, then due today. Each item: plant nickname in `text-sm font-medium text-foreground`, then room + status on a second line in `text-xs text-muted-foreground`. No dividers between items. Clicking any item navigates to `/plants/{plantId}`.
- **Empty dropdown:** Single non-interactive row: "All caught up!" in `text-sm text-muted-foreground px-md py-sm`.

### Snooze Pills (Plant Detail Page)

- **Visibility condition:** Pill row renders only when plant status is overdue or due today (computed server-side, passed as prop).
- **Pill layout:** Horizontal row of 4 pills: `1d`, `2d`, `1w`, `Custom`. Use `Button size="sm" variant="outline"` for each. `gap-sm` between pills. Row label above: `text-xs text-muted-foreground` reading "Snooze reminder:".
- **Snooze confirmation:** On pill click (`1d`, `2d`, `1w`), Server Action fires immediately. Toast: "Reminder snoozed for {N} day(s)." using Sonner. No confirmation dialog for quick snooze durations.
- **Custom snooze flow:** Clicking "Custom" opens a Dialog (not a popover) with a Calendar date picker. Dialog title: "Choose snooze date". Calendar shows today as the minimum selectable date. Cancel button (outline) and "Snooze" button (default). On confirm, Server Action fires and toast fires: "Reminder snoozed until {date}."
- **Post-snooze state:** Pills disappear (plant no longer qualifies as overdue/due-today from the reminder perspective). The plant's status label on the detail page still reflects its actual watering status — snooze only affects the notification badge and dropdown, not the urgency classification display.

### Global Reminders Toggle (Preferences Page)

- **Component:** shadcn Switch with a label. `Switch` to the left of the label text. Label: "In-app reminders". Helper text below: "When on, plants needing water appear in your notification center." in `text-xs text-muted-foreground`.
- **Behavior:** Optimistic toggle — Switch updates immediately, Server Action fires in background. On error, Switch reverts to previous state and toast shows error copy.
- **Initial state:** On by default (D-07).

### Per-Plant Reminder Toggle (Plant Detail Page)

- **Component:** Same Switch pattern as global toggle. Label: "Reminders for this plant". Positioned in the plant care settings section of the detail page.
- **Behavior:** Same optimistic toggle pattern as global toggle.
- **Dependency:** If global reminders are off, the per-plant Switch is visible but shows a helper text: "Enable reminders globally in Preferences to activate." Switch is non-interactive (disabled, not hidden) when global reminders are off.

### Preferences Page (/preferences)

- **Page layout:** Single column, `max-w-xl mx-auto`. Two Card sections: "Reminders" and "Account". `gap-lg` between cards.
- **Nav integration:** Add "Preferences" text link to the right action group of the nav (between "Complete setup" conditional link and the email display). `text-sm font-medium text-muted-foreground hover:text-foreground`.
- **Reminders section:** Card with Switch component (global toggle). Save is implicit on toggle — no Save button needed.
- **Account section:** Card with sub-sections: Change Email (form: current password + new email), Change Password (form: current password + new password + confirm), Delete Account (destructive zone with AlertDialog).

### Demo Mode

- **Entry point:** "Try demo" link on the `/login` page. Position: below the sign-in form, center-aligned. Style: `text-sm text-accent hover:underline`. Copy: "Explore without signing up".
- **Demo banner:** Sticky top bar within the `(main)` layout when `session.user.isDemo === true`. Height: 36px (9 * 4px). Background: `bg-surface border-b border-border`. Content: `text-sm text-muted-foreground` reading "You're in demo mode — " followed by an anchor: `Sign up to save your data` linking to `/register` in `text-accent hover:underline`. Positioned above the existing header, not inside it.
- **Read-only enforcement:** Mutation buttons (Water, Add Plant, Add Note, etc.) are visible but disabled (`disabled` prop) when `isDemo`. A tooltip on hover: "Sign up to save changes." This matches DEMO-02 (mutations blocked) while still communicating the app's value.
- **Starter plant seeding (onboarding, DEMO-03):** Checkbox opt-in in the existing onboarding banner. Copy: "Start with a few example plants" with a checkbox. Checked by default. Fires a Server Action on onboarding completion.

---

## Copywriting Contract

### Notification Center

| Element | Copy |
|---------|------|
| Badge aria-label (count > 0) | "{count} plants need attention" |
| Badge aria-label (count = 0) | "No plants need attention" |
| Dropdown empty state | "All caught up!" |
| Dropdown item status — overdue | "{N} day{s} overdue" (e.g., "2 days overdue", "1 day overdue") |
| Dropdown item status — due today | "Due today" |

### Snooze

| Element | Copy |
|---------|------|
| Snooze row label | "Snooze reminder:" |
| Snooze pill labels | "1d", "2d", "1w", "Custom" |
| Custom dialog title | "Choose snooze date" |
| Custom dialog cancel | "Cancel" |
| Custom dialog confirm | "Snooze" |
| Toast — quick snooze (1d) | "Reminder snoozed for 1 day." |
| Toast — quick snooze (2d) | "Reminder snoozed for 2 days." |
| Toast — quick snooze (1w) | "Reminder snoozed for 1 week." |
| Toast — custom snooze | "Reminder snoozed until {MMM d}." (e.g., "Reminder snoozed until Apr 22.") |
| Error toast (snooze failed) | "Could not snooze reminder. Try again." |

### Reminder Toggles

| Element | Copy |
|---------|------|
| Global toggle label | "In-app reminders" |
| Global toggle helper text | "When on, plants needing water appear in your notification center." |
| Per-plant toggle label | "Reminders for this plant" |
| Per-plant toggle disabled helper | "Enable reminders globally in Preferences to activate." |
| Toast — toggle saved | "Preferences saved." |
| Error toast — toggle failed | "Could not save preferences. Try again." |

### Preferences Page

| Element | Copy |
|---------|------|
| Page title (h1) | "Preferences" |
| Reminders card heading | "Notifications" |
| Account card heading | "Account" |
| Change email section label | "Email address" |
| Change password section label | "Password" |
| Change email button | "Update email" |
| Change password button | "Update password" |
| Toast — email updated | "Email updated. Check your inbox to confirm." |
| Toast — password updated | "Password updated." |
| Error — wrong current password | "Current password is incorrect." |
| Delete account button | "Delete account" (destructive variant) |
| Delete account dialog title | "Delete your account?" |
| Delete account dialog body | "This will permanently delete all your plants, watering history, and notes. This cannot be undone." |
| Delete account dialog cancel | "Cancel" |
| Delete account dialog confirm | "Yes, delete my account" (destructive) |

### Demo Mode

| Element | Copy |
|---------|------|
| Login page CTA | "Explore without signing up" |
| Demo banner text | "You're in demo mode —" |
| Demo banner link | "Sign up to save your data" |
| Disabled action tooltip | "Sign up to save changes." |
| Starter plants checkbox label | "Start with a few example plants" |
| Onboarding confirm button | "Get started" (no change from existing) |

### Empty States

| Context | Heading | Body |
|---------|---------|------|
| Notification dropdown (no plants need water) | — (no heading, inline message only) | "All caught up!" |
| Preferences page (no plants = no per-plant toggles to show) | — | Not applicable; page shows static sections |

### Error States

| Context | Copy |
|---------|------|
| Snooze Server Action failure | "Could not snooze reminder. Try again." |
| Toggle Server Action failure | "Could not save preferences. Try again." |
| Delete account Server Action failure | "Could not delete account. Contact support if this continues." |
| Demo session expired | Redirect to /login — no toast needed |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | DropdownMenu, Badge, Button, Switch, Dialog, AlertDialog, Popover, Calendar, Card, Input, Form, Separator, Sonner | not required |

No third-party registries declared for this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

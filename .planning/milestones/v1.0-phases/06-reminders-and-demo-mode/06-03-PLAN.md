---
phase: 06-reminders-and-demo-mode
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/components/reminders/notification-bell.tsx
  - src/components/reminders/snooze-pills.tsx
  - src/components/reminders/plant-reminder-toggle.tsx
  - src/app/(main)/layout.tsx
  - src/components/plants/plant-detail.tsx
  - src/app/(main)/plants/[id]/page.tsx
  - src/app/(main)/preferences/page.tsx
  - src/components/preferences/preferences-form.tsx
  - src/components/preferences/account-settings.tsx
autonomous: true
requirements:
  - RMDR-01
  - RMDR-02
  - RMDR-03
  - RMDR-04
  - RMDR-05

must_haves:
  truths:
    - "Nav bar shows a bell icon with a badge count of plants needing attention"
    - "Clicking the bell opens a dropdown listing overdue and due-today plants"
    - "Clicking a dropdown item navigates to that plant's detail page"
    - "Empty dropdown shows 'All caught up!'"
    - "Overdue/due-today plants show snooze pills on their detail page"
    - "Snooze pills fire Server Actions and show toast confirmations"
    - "Per-plant reminder toggle appears on plant detail page"
    - "Per-plant toggle is disabled (not hidden) when global reminders are off"
    - "/preferences page shows global reminders toggle and account settings"
    - "Preferences link appears in the nav"
  artifacts:
    - path: "src/components/reminders/notification-bell.tsx"
      provides: "Bell icon with badge and dropdown"
      contains: "NotificationBell"
    - path: "src/components/reminders/snooze-pills.tsx"
      provides: "Snooze pill buttons with 1d/2d/1w/Custom"
      contains: "SnoozePills"
    - path: "src/components/reminders/plant-reminder-toggle.tsx"
      provides: "Per-plant reminder Switch toggle"
      contains: "PlantReminderToggle"
    - path: "src/app/(main)/preferences/page.tsx"
      provides: "Preferences page route"
      contains: "PreferencesForm"
    - path: "src/components/preferences/preferences-form.tsx"
      provides: "Global toggle and account settings"
      contains: "PreferencesForm"
  key_links:
    - from: "src/app/(main)/layout.tsx"
      to: "src/features/reminders/queries.ts"
      via: "getReminderCount and getReminderItems calls"
      pattern: "getReminderCount|getReminderItems"
    - from: "src/components/reminders/snooze-pills.tsx"
      to: "src/features/reminders/actions.ts"
      via: "snoozeReminder import and call"
      pattern: "snoozeReminder"
    - from: "src/components/preferences/preferences-form.tsx"
      to: "src/features/reminders/actions.ts"
      via: "toggleGlobalReminders import"
      pattern: "toggleGlobalReminders"
---

<objective>
Build all reminder UI components and the preferences page. The notification bell with badge and dropdown goes in the nav. Snooze pills and per-plant reminder toggle go on the plant detail page. The preferences page provides the global reminders toggle and account settings.

Purpose: This delivers the visible reminder experience — the bell icon that shows badge count (RMDR-01/02), the preferences page for global toggle (RMDR-03), per-plant toggle (RMDR-04), and snooze pills (RMDR-05). Users can see, configure, and interact with the full reminder system.

Output: NotificationBell, SnoozePills, PlantReminderToggle components; updated layout.tsx and plant-detail.tsx; new /preferences route.
</objective>

<execution_context>
@c:/Dev/poc/plantz-gsd/.claude/get-shit-done/workflows/execute-plan.md
@c:/Dev/poc/plantz-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-reminders-and-demo-mode/06-CONTEXT.md
@.planning/phases/06-reminders-and-demo-mode/06-RESEARCH.md
@.planning/phases/06-reminders-and-demo-mode/06-UI-SPEC.md
@.planning/phases/06-reminders-and-demo-mode/06-01-SUMMARY.md
@.planning/phases/06-reminders-and-demo-mode/06-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plans 01-02 and existing codebase. -->

From src/features/reminders/queries.ts (created in Plan 02):
```typescript
export async function getReminderCount(userId: string, todayStart: Date, todayEnd: Date): Promise<number>;
export async function getReminderItems(userId: string, todayStart: Date, todayEnd: Date): Promise<ReminderItem[]>;
export async function getPlantReminder(plantId: string, userId: string): Promise<{ enabled: boolean; snoozedUntil: Date | null } | null>;
```

From src/features/reminders/actions.ts (created in Plan 02):
```typescript
export async function snoozeReminder(data: unknown): Promise<{ success: true } | { error: string }>;
export async function snoozeCustomReminder(data: unknown): Promise<{ success: true } | { error: string }>;
export async function togglePlantReminder(data: unknown): Promise<{ success: true } | { error: string }>;
export async function toggleGlobalReminders(data: unknown): Promise<{ success: true } | { error: string }>;
```

From src/features/reminders/types.ts (created in Plan 01):
```typescript
export interface ReminderItem {
  plantId: string;
  nickname: string;
  roomName: string | null;
  statusLabel: string;
  daysOverdue: number;
}
```

From src/app/(main)/layout.tsx (CURRENT — will be modified):
```typescript
// Server Component with auth(), db query, and nav rendering
// Nav structure: logo left, Plants/Rooms center, actions right
// Right actions: "Complete setup" link (conditional), email, LogoutButton
```

From src/components/plants/plant-detail.tsx (CURRENT — will be modified):
```typescript
// Client component with PlantDetailProps: plant, timelineEntries, timelineTotal
// Has wateringStatus computed: "overdue" | "due-today" | "upcoming" | "not-scheduled"
// Status card → Care info card → Timeline card
```

From src/app/(main)/plants/[id]/page.tsx (CURRENT — will be modified to pass reminder data):
```typescript
// Server Component that fetches plant and passes to PlantDetail
```

From src/components/ui/switch.tsx (installed in Plan 01):
```typescript
// shadcn Switch primitive — controlled via checked/onCheckedChange props
```

From src/components/ui/dropdown-menu.tsx (existing):
```typescript
export { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, ... }
```

From globals.css spacing tokens:
```
xs=4px, sm=8px, md=16px, lg=24px, xl=32px
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NotificationBell component and integrate into nav layout</name>
  <files>
    src/components/reminders/notification-bell.tsx,
    src/app/(main)/layout.tsx
  </files>
  <read_first>
    src/app/(main)/layout.tsx,
    src/components/ui/dropdown-menu.tsx,
    src/components/ui/badge.tsx,
    src/features/reminders/queries.ts,
    src/features/reminders/types.ts,
    src/app/(main)/dashboard/page.tsx,
    .planning/phases/06-reminders-and-demo-mode/06-UI-SPEC.md
  </read_first>
  <action>
**1. Create src/components/reminders/notification-bell.tsx:**

Client component that renders the bell icon, badge overlay, and dropdown panel per UI-SPEC interaction contracts:

```typescript
"use client";

import { Bell } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuTrigger,
  DropdownMenuItem,
} from "@/components/ui/dropdown-menu";
import { Button } from "@/components/ui/button";
import { useRouter } from "next/navigation";
import type { ReminderItem } from "@/features/reminders/types";

interface NotificationBellProps {
  count: number;
  items: ReminderItem[];
}

export function NotificationBell({ count, items }: NotificationBellProps) {
  const router = useRouter();

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button
          variant="ghost"
          size="icon"
          className="relative p-2.5"
          aria-label={
            count > 0
              ? `${count} plants need attention`
              : "No plants need attention"
          }
        >
          <Bell
            className={`h-5 w-5 ${count > 0 ? "text-foreground" : "text-muted-foreground"}`}
          />
          {count > 0 && (
            <Badge
              className="absolute -top-1 -right-1 flex h-4 min-w-4 items-center justify-center rounded-full bg-accent px-1 py-0 text-[10px] font-semibold text-accent-foreground"
            >
              {count > 99 ? "99+" : count}
            </Badge>
          )}
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end" className="w-72 max-h-[320px] overflow-y-auto">
        {items.length === 0 ? (
          <p className="px-4 py-2 text-sm text-muted-foreground">
            All caught up!
          </p>
        ) : (
          items.map((item) => (
            <DropdownMenuItem
              key={item.plantId}
              onClick={() => router.push(`/plants/${item.plantId}`)}
              className="flex cursor-pointer flex-col items-start gap-1 py-2"
            >
              <span className="text-sm font-semibold text-foreground">
                {item.nickname}
              </span>
              <span className="text-xs text-muted-foreground">
                {item.roomName ?? "No room"} &middot; {item.statusLabel}
              </span>
            </DropdownMenuItem>
          ))
        )}
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

**2. Update src/app/(main)/layout.tsx:**

Add reminder count + items query and NotificationBell to the nav. Import `getReminderCount` and `getReminderItems` from `@/features/reminders/queries`. Import `NotificationBell` from `@/components/reminders/notification-bell`. Import `cookies` from `next/headers` to read the `user_tz` cookie for timezone-aware today boundaries.

Read the `user_tz` cookie (same pattern as `dashboard/page.tsx`). Compute `todayStart` and `todayEnd` from the timezone offset. If no cookie, default to UTC.

Add the query calls to the layout function body (after `user` fetch, before the return):

```typescript
import { cookies } from "next/headers";
import { getReminderCount, getReminderItems } from "@/features/reminders/queries";
import { NotificationBell } from "@/components/reminders/notification-bell";

// Inside the layout function, after the existing user query:
const cookieStore = await cookies();
const tzOffset = parseInt(cookieStore.get("user_tz")?.value ?? "0", 10);
const now = new Date();
const todayStart = new Date(now);
todayStart.setUTCHours(0, 0, 0, 0);
todayStart.setUTCMinutes(todayStart.getUTCMinutes() + tzOffset);
const todayEnd = new Date(todayStart);
todayEnd.setUTCDate(todayEnd.getUTCDate() + 1);

const [reminderCount, reminderItems] = await Promise.all([
  getReminderCount(session.user.id, todayStart, todayEnd),
  getReminderItems(session.user.id, todayStart, todayEnd),
]);
```

In the JSX, add the `NotificationBell` and a "Preferences" link to the right actions group. The bell goes between the nav links and the user info. The Preferences link goes between "Complete setup" and the email. Updated right section:

```tsx
<div className="flex items-center gap-md">
  <NotificationBell count={reminderCount} items={reminderItems} />
  {!user?.onboardingCompleted && (
    <Link
      href="/dashboard#onboarding"
      className="text-sm font-medium text-accent hover:underline"
    >
      Complete setup
    </Link>
  )}
  <Link
    href="/preferences"
    className="text-sm font-semibold text-muted-foreground hover:text-foreground"
  >
    Preferences
  </Link>
  <span className="text-sm text-muted-foreground">{user?.email}</span>
  <LogoutButton />
</div>
```

Also add `remindersEnabled` to the user query select (for passing to downstream components):

```typescript
const user = await db.user.findUnique({
  where: { id: session.user.id },
  select: { email: true, onboardingCompleted: true, remindersEnabled: true },
});
```
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/reminders/notification-bell.tsx contains `export function NotificationBell(`
    - src/components/reminders/notification-bell.tsx contains `"use client"`
    - src/components/reminders/notification-bell.tsx contains `aria-label=` with plant count
    - src/components/reminders/notification-bell.tsx contains `All caught up!`
    - src/components/reminders/notification-bell.tsx contains `router.push(`/plants/${item.plantId}`)`
    - src/components/reminders/notification-bell.tsx contains `99+`
    - src/app/(main)/layout.tsx contains `import { NotificationBell }` 
    - src/app/(main)/layout.tsx contains `getReminderCount`
    - src/app/(main)/layout.tsx contains `getReminderItems`
    - src/app/(main)/layout.tsx contains `<NotificationBell`
    - src/app/(main)/layout.tsx contains `href="/preferences"`
    - src/app/(main)/layout.tsx contains `remindersEnabled: true` in user select
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>NotificationBell renders in the nav with badge count (per D-01, D-02), dropdown panel with plant items (per D-03), navigation to plant detail on click (per D-04), and empty state "All caught up!" text. Preferences link added to nav (per D-05).</done>
</task>

<task type="auto">
  <name>Task 2: Create SnoozePills and PlantReminderToggle, integrate into plant detail page</name>
  <files>
    src/components/reminders/snooze-pills.tsx,
    src/components/reminders/plant-reminder-toggle.tsx,
    src/components/plants/plant-detail.tsx,
    src/app/(main)/plants/[id]/page.tsx
  </files>
  <read_first>
    src/components/plants/plant-detail.tsx,
    src/app/(main)/plants/[id]/page.tsx,
    src/features/reminders/actions.ts,
    src/features/reminders/queries.ts,
    src/components/ui/switch.tsx,
    src/components/ui/button.tsx,
    src/components/ui/dialog.tsx,
    src/components/ui/calendar.tsx,
    .planning/phases/06-reminders-and-demo-mode/06-UI-SPEC.md
  </read_first>
  <action>
**1. Create src/components/reminders/snooze-pills.tsx:**

Client component rendering the 4 snooze pill buttons per D-08 and UI-SPEC. "1d", "2d", "1w" fire `snoozeReminder` immediately. "Custom" opens a Dialog with a Calendar date picker per UI-SPEC.

```typescript
"use client";

import { useState } from "react";
import { Button } from "@/components/ui/button";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogFooter,
} from "@/components/ui/dialog";
import { Calendar } from "@/components/ui/calendar";
import { snoozeReminder, snoozeCustomReminder } from "@/features/reminders/actions";
import { toast } from "sonner";
import { format, addDays } from "date-fns";

const QUICK_SNOOZE_OPTIONS = [
  { label: "1d", days: 1, toastMsg: "Reminder snoozed for 1 day." },
  { label: "2d", days: 2, toastMsg: "Reminder snoozed for 2 days." },
  { label: "1w", days: 7, toastMsg: "Reminder snoozed for 1 week." },
] as const;

interface SnoozePillsProps {
  plantId: string;
  isDemo?: boolean;
}

export function SnoozePills({ plantId, isDemo }: SnoozePillsProps) {
  const [isCustomOpen, setIsCustomOpen] = useState(false);
  const [selectedDate, setSelectedDate] = useState<Date | undefined>(undefined);
  const [isPending, setIsPending] = useState(false);

  async function handleQuickSnooze(days: number, toastMsg: string) {
    if (isDemo) {
      toast.error("Sign up to save changes.");
      return;
    }
    setIsPending(true);
    const result = await snoozeReminder({ plantId, days });
    setIsPending(false);
    if (result?.error) {
      toast.error("Could not snooze reminder. Try again.");
    } else {
      toast.success(toastMsg);
    }
  }

  async function handleCustomSnooze() {
    if (!selectedDate || isDemo) {
      if (isDemo) toast.error("Sign up to save changes.");
      return;
    }
    setIsPending(true);
    const result = await snoozeCustomReminder({ plantId, snoozedUntil: selectedDate });
    setIsPending(false);
    if (result?.error) {
      toast.error("Could not snooze reminder. Try again.");
    } else {
      toast.success(`Reminder snoozed until ${format(selectedDate, "MMM d")}.`);
      setIsCustomOpen(false);
      setSelectedDate(undefined);
    }
  }

  return (
    <>
      <div className="space-y-1">
        <span className="text-xs text-muted-foreground">Snooze reminder:</span>
        <div className="flex items-center gap-2">
          {QUICK_SNOOZE_OPTIONS.map(({ label, days, toastMsg }) => (
            <Button
              key={label}
              variant="outline"
              size="sm"
              className="h-7 px-2 text-xs"
              onClick={() => handleQuickSnooze(days, toastMsg)}
              disabled={isPending || isDemo}
            >
              {label}
            </Button>
          ))}
          <Button
            variant="outline"
            size="sm"
            className="h-7 px-2 text-xs"
            onClick={() => setIsCustomOpen(true)}
            disabled={isPending || isDemo}
          >
            Custom
          </Button>
        </div>
      </div>

      <Dialog open={isCustomOpen} onOpenChange={setIsCustomOpen}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>Choose snooze date</DialogTitle>
          </DialogHeader>
          <div className="flex justify-center py-4">
            <Calendar
              mode="single"
              selected={selectedDate}
              onSelect={setSelectedDate}
              disabled={(date) => date <= new Date()}
              initialFocus
            />
          </div>
          <DialogFooter className="gap-2 sm:gap-0">
            <Button
              variant="outline"
              onClick={() => {
                setIsCustomOpen(false);
                setSelectedDate(undefined);
              }}
            >
              Keep current schedule
            </Button>
            <Button
              onClick={handleCustomSnooze}
              disabled={!selectedDate || isPending}
            >
              Snooze reminder
            </Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>
    </>
  );
}
```

**2. Create src/components/reminders/plant-reminder-toggle.tsx:**

Client component with the per-plant reminder Switch per D-05 and UI-SPEC:

```typescript
"use client";

import { useState } from "react";
import { Switch } from "@/components/ui/switch";
import { togglePlantReminder } from "@/features/reminders/actions";
import { toast } from "sonner";

interface PlantReminderToggleProps {
  plantId: string;
  initialEnabled: boolean;
  globalRemindersEnabled: boolean;
  isDemo?: boolean;
}

export function PlantReminderToggle({
  plantId,
  initialEnabled,
  globalRemindersEnabled,
  isDemo,
}: PlantReminderToggleProps) {
  const [enabled, setEnabled] = useState(initialEnabled);
  const [isPending, setIsPending] = useState(false);

  async function handleToggle(checked: boolean) {
    if (isDemo) {
      toast.error("Sign up to save changes.");
      return;
    }

    // Optimistic update
    setEnabled(checked);
    setIsPending(true);

    const result = await togglePlantReminder({ plantId, enabled: checked });
    setIsPending(false);

    if (result?.error) {
      // Revert on error
      setEnabled(!checked);
      toast.error("Could not save preferences. Try again.");
    } else {
      toast.success("Preferences saved.");
    }
  }

  const isDisabled = !globalRemindersEnabled || isPending || !!isDemo;

  return (
    <div className="flex items-center justify-between">
      <div className="space-y-0.5">
        <label
          htmlFor="plant-reminder-toggle"
          className="text-sm font-semibold"
        >
          Reminders for this plant
        </label>
        {!globalRemindersEnabled && (
          <p className="text-xs text-muted-foreground">
            Enable reminders globally in Preferences to activate.
          </p>
        )}
      </div>
      <Switch
        id="plant-reminder-toggle"
        checked={enabled}
        onCheckedChange={handleToggle}
        disabled={isDisabled}
        aria-label="Reminders for this plant"
      />
    </div>
  );
}
```

**3. Update src/components/plants/plant-detail.tsx:**

Add `SnoozePills` and `PlantReminderToggle` to the plant detail page. The props interface must be extended with new fields:

Add to PlantDetailProps:
```typescript
interface PlantDetailProps {
  plant: PlantWithRelations;
  timelineEntries: TimelineEntryType[];
  timelineTotal: number;
  reminderEnabled: boolean;
  globalRemindersEnabled: boolean;
  isDemo?: boolean;
}
```

Import and render `SnoozePills` inside the status card, after the overdue/due-today badge display:

```typescript
import { SnoozePills } from "@/components/reminders/snooze-pills";
import { PlantReminderToggle } from "@/components/reminders/plant-reminder-toggle";
```

In the JSX, add SnoozePills after the overdue Badge:
```tsx
{wateringStatus === "overdue" && (
  <div className="space-y-3">
    <div className="flex items-center gap-sm">
      <Badge variant="destructive">Overdue</Badge>
      <span className="text-sm text-destructive">
        {Math.abs(daysUntilWatering)} day{Math.abs(daysUntilWatering) !== 1 ? "s" : ""} overdue
      </span>
    </div>
    <SnoozePills plantId={plant.id} isDemo={isDemo} />
  </div>
)}
{wateringStatus === "due-today" && (
  <div className="space-y-3">
    <div className="flex items-center gap-sm">
      <Badge variant="default">Due today</Badge>
      {nextWatering && (
        <span className="text-sm text-muted-foreground">
          {format(nextWatering, "MMMM d, yyyy")}
        </span>
      )}
    </div>
    <SnoozePills plantId={plant.id} isDemo={isDemo} />
  </div>
)}
```

Add the `PlantReminderToggle` as a new section between the "Care info" card and the "Timeline" card:

```tsx
{/* Reminder settings */}
<Card>
  <CardHeader>
    <CardTitle>Reminder settings</CardTitle>
  </CardHeader>
  <CardContent>
    <PlantReminderToggle
      plantId={plant.id}
      initialEnabled={reminderEnabled}
      globalRemindersEnabled={globalRemindersEnabled}
      isDemo={isDemo}
    />
  </CardContent>
</Card>
```

**4. Update src/app/(main)/plants/[id]/page.tsx:**

Read the file first to understand its current structure. Add `getPlantReminder` import and call. Import `db` for user `remindersEnabled` lookup. Pass the new props (`reminderEnabled`, `globalRemindersEnabled`, `isDemo`) to `PlantDetail`:

```typescript
import { getPlantReminder } from "@/features/reminders/queries";

// Inside the page function, after plant fetch:
const [reminder, user] = await Promise.all([
  getPlantReminder(plant.id, session.user.id),
  db.user.findUnique({
    where: { id: session.user.id },
    select: { remindersEnabled: true },
  }),
]);

// Pass to PlantDetail:
<PlantDetail
  plant={plant}
  timelineEntries={...}
  timelineTotal={...}
  reminderEnabled={reminder?.enabled ?? true}
  globalRemindersEnabled={user?.remindersEnabled ?? true}
  isDemo={session.user.isDemo ?? false}
/>
```
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/reminders/notification-bell.tsx exists and is validated by previous task
    - src/components/reminders/snooze-pills.tsx contains `export function SnoozePills(`
    - src/components/reminders/snooze-pills.tsx contains `"use client"`
    - src/components/reminders/snooze-pills.tsx contains `snoozeReminder` import
    - src/components/reminders/snooze-pills.tsx contains `snoozeCustomReminder` import
    - src/components/reminders/snooze-pills.tsx contains `"1d"` and `"2d"` and `"1w"` and `"Custom"` button labels
    - src/components/reminders/snooze-pills.tsx contains `Choose snooze date` dialog title
    - src/components/reminders/snooze-pills.tsx contains `Keep current schedule` button text
    - src/components/reminders/snooze-pills.tsx contains `Snooze reminder` button text
    - src/components/reminders/plant-reminder-toggle.tsx contains `export function PlantReminderToggle(`
    - src/components/reminders/plant-reminder-toggle.tsx contains `Switch` import
    - src/components/reminders/plant-reminder-toggle.tsx contains `togglePlantReminder` import
    - src/components/reminders/plant-reminder-toggle.tsx contains `Enable reminders globally in Preferences to activate.`
    - src/components/plants/plant-detail.tsx contains `SnoozePills` import
    - src/components/plants/plant-detail.tsx contains `PlantReminderToggle` import
    - src/components/plants/plant-detail.tsx contains `reminderEnabled` in props
    - src/components/plants/plant-detail.tsx contains `globalRemindersEnabled` in props
    - src/app/(main)/plants/[id]/page.tsx contains `getPlantReminder` import
    - src/app/(main)/plants/[id]/page.tsx contains `isDemo` prop
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SnoozePills show on overdue/due-today plants with 1d/2d/1w/Custom options (per D-08). Custom opens a Calendar Dialog (per UI-SPEC). PlantReminderToggle shows on plant detail with optimistic switch (per D-05). Toggle disabled when global reminders off (per UI-SPEC). All wired to Server Actions from Plan 02.</done>
</task>

<task type="auto">
  <name>Task 3: Create preferences page with global reminders toggle and account settings</name>
  <files>
    src/app/(main)/preferences/page.tsx,
    src/components/preferences/preferences-form.tsx,
    src/components/preferences/account-settings.tsx
  </files>
  <read_first>
    src/features/reminders/actions.ts,
    src/features/auth/actions.ts,
    src/components/ui/switch.tsx,
    src/components/ui/card.tsx,
    src/components/ui/input.tsx,
    src/components/ui/form.tsx,
    src/components/ui/separator.tsx,
    src/components/ui/alert-dialog.tsx,
    src/components/auth/login-form.tsx,
    .planning/phases/06-reminders-and-demo-mode/06-UI-SPEC.md
  </read_first>
  <action>
**1. Create src/components/preferences/preferences-form.tsx:**

Client component with the global reminders Switch (per D-05, D-06) and account settings sections:

```typescript
"use client";

import { useState } from "react";
import { Switch } from "@/components/ui/switch";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Separator } from "@/components/ui/separator";
import { toggleGlobalReminders } from "@/features/reminders/actions";
import { toast } from "sonner";
import { AccountSettings } from "./account-settings";

interface PreferencesFormProps {
  initialRemindersEnabled: boolean;
  userEmail: string;
  isDemo?: boolean;
}

export function PreferencesForm({
  initialRemindersEnabled,
  userEmail,
  isDemo,
}: PreferencesFormProps) {
  const [remindersEnabled, setRemindersEnabled] = useState(initialRemindersEnabled);
  const [isPending, setIsPending] = useState(false);

  async function handleToggle(checked: boolean) {
    if (isDemo) {
      toast.error("Sign up to save changes.");
      return;
    }

    // Optimistic toggle
    setRemindersEnabled(checked);
    setIsPending(true);

    const result = await toggleGlobalReminders({ enabled: checked });
    setIsPending(false);

    if (result?.error) {
      setRemindersEnabled(!checked);
      toast.error("Could not save preferences. Try again.");
    } else {
      toast.success("Preferences saved.");
    }
  }

  return (
    <div className="mx-auto max-w-xl space-y-6">
      <h1 className="text-xl font-semibold">Preferences</h1>

      {/* Notifications section — per D-06 */}
      <Card>
        <CardHeader>
          <CardTitle>Notifications</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="flex items-center justify-between">
            <div className="space-y-0.5">
              <label
                htmlFor="global-reminders-toggle"
                className="text-sm font-semibold"
              >
                In-app reminders
              </label>
              <p className="text-xs text-muted-foreground">
                When on, plants needing water appear in your notification center.
              </p>
            </div>
            <Switch
              id="global-reminders-toggle"
              checked={remindersEnabled}
              onCheckedChange={handleToggle}
              disabled={isPending || !!isDemo}
              aria-label="In-app reminders"
            />
          </div>
        </CardContent>
      </Card>

      {/* Account section — per D-06 */}
      <Card>
        <CardHeader>
          <CardTitle>Account</CardTitle>
        </CardHeader>
        <CardContent>
          <AccountSettings userEmail={userEmail} isDemo={isDemo} />
        </CardContent>
      </Card>
    </div>
  );
}
```

**2. Create src/components/preferences/account-settings.tsx:**

Client component with change email, change password, and delete account sections per D-06 and UI-SPEC copywriting contract:

```typescript
"use client";

import { useState } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { z } from "zod/v4";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Separator } from "@/components/ui/separator";
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from "@/components/ui/form";
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/alert-dialog";
import { toast } from "sonner";
import { Loader2 } from "lucide-react";

const changeEmailSchema = z.object({
  currentPassword: z.string().min(1, "Current password is required."),
  newEmail: z.email("Please enter a valid email."),
});

const changePasswordSchema = z.object({
  currentPassword: z.string().min(1, "Current password is required."),
  newPassword: z.string().min(8, "Password must be at least 8 characters."),
  confirmPassword: z.string().min(1, "Please confirm your password."),
}).refine((data) => data.newPassword === data.confirmPassword, {
  message: "Passwords do not match.",
  path: ["confirmPassword"],
});

type ChangeEmailInput = z.infer<typeof changeEmailSchema>;
type ChangePasswordInput = z.infer<typeof changePasswordSchema>;

interface AccountSettingsProps {
  userEmail: string;
  isDemo?: boolean;
}

export function AccountSettings({ userEmail, isDemo }: AccountSettingsProps) {
  const [isDeleting, setIsDeleting] = useState(false);

  const emailForm = useForm<ChangeEmailInput>({
    resolver: zodResolver(changeEmailSchema),
    defaultValues: { currentPassword: "", newEmail: "" },
  });

  const passwordForm = useForm<ChangePasswordInput>({
    resolver: zodResolver(changePasswordSchema),
    defaultValues: { currentPassword: "", newPassword: "", confirmPassword: "" },
  });

  async function onEmailSubmit(values: ChangeEmailInput) {
    if (isDemo) { toast.error("Sign up to save changes."); return; }

    // Server Action call — to be implemented in Phase 7 or later
    // For now, the form validates and shows a toast
    toast.info("Email update is not yet available.");
    emailForm.reset();
  }

  async function onPasswordSubmit(values: ChangePasswordInput) {
    if (isDemo) { toast.error("Sign up to save changes."); return; }

    // Server Action call — to be implemented in Phase 7 or later
    toast.info("Password update is not yet available.");
    passwordForm.reset();
  }

  async function handleDeleteAccount() {
    if (isDemo) { toast.error("Sign up to save changes."); return; }

    setIsDeleting(true);
    // Server Action call — to be implemented in Phase 7 or later
    toast.info("Account deletion is not yet available.");
    setIsDeleting(false);
  }

  return (
    <div className="space-y-6">
      {/* Change Email */}
      <div className="space-y-3">
        <h3 className="text-sm font-semibold">Email address</h3>
        <p className="text-xs text-muted-foreground">Current: {userEmail}</p>
        <Form {...emailForm}>
          <form onSubmit={emailForm.handleSubmit(onEmailSubmit)} className="space-y-3">
            <FormField
              control={emailForm.control}
              name="currentPassword"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Current password</FormLabel>
                  <FormControl>
                    <Input type="password" {...field} disabled={!!isDemo} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <FormField
              control={emailForm.control}
              name="newEmail"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>New email</FormLabel>
                  <FormControl>
                    <Input type="email" {...field} disabled={!!isDemo} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <Button
              type="submit"
              size="sm"
              disabled={emailForm.formState.isSubmitting || !!isDemo}
            >
              {emailForm.formState.isSubmitting ? (
                <><Loader2 className="mr-2 h-4 w-4 animate-spin" />Updating...</>
              ) : (
                "Update email"
              )}
            </Button>
          </form>
        </Form>
      </div>

      <Separator />

      {/* Change Password */}
      <div className="space-y-3">
        <h3 className="text-sm font-semibold">Password</h3>
        <Form {...passwordForm}>
          <form onSubmit={passwordForm.handleSubmit(onPasswordSubmit)} className="space-y-3">
            <FormField
              control={passwordForm.control}
              name="currentPassword"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Current password</FormLabel>
                  <FormControl>
                    <Input type="password" {...field} disabled={!!isDemo} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <FormField
              control={passwordForm.control}
              name="newPassword"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>New password</FormLabel>
                  <FormControl>
                    <Input type="password" {...field} disabled={!!isDemo} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <FormField
              control={passwordForm.control}
              name="confirmPassword"
              render={({ field }) => (
                <FormItem>
                  <FormLabel>Confirm new password</FormLabel>
                  <FormControl>
                    <Input type="password" {...field} disabled={!!isDemo} />
                  </FormControl>
                  <FormMessage />
                </FormItem>
              )}
            />
            <Button
              type="submit"
              size="sm"
              disabled={passwordForm.formState.isSubmitting || !!isDemo}
            >
              {passwordForm.formState.isSubmitting ? (
                <><Loader2 className="mr-2 h-4 w-4 animate-spin" />Updating...</>
              ) : (
                "Update password"
              )}
            </Button>
          </form>
        </Form>
      </div>

      <Separator />

      {/* Delete Account */}
      <div className="space-y-3">
        <AlertDialog>
          <AlertDialogTrigger asChild>
            <Button variant="destructive" size="sm" disabled={!!isDemo}>
              Delete account
            </Button>
          </AlertDialogTrigger>
          <AlertDialogContent>
            <AlertDialogHeader>
              <AlertDialogTitle>Delete your account?</AlertDialogTitle>
              <AlertDialogDescription>
                This will permanently delete all your plants, watering history, and notes. This cannot be undone.
              </AlertDialogDescription>
            </AlertDialogHeader>
            <AlertDialogFooter>
              <AlertDialogCancel>Keep my account</AlertDialogCancel>
              <AlertDialogAction
                onClick={handleDeleteAccount}
                disabled={isDeleting}
                className="bg-destructive text-destructive-foreground hover:bg-destructive/90"
              >
                {isDeleting ? "Deleting..." : "Yes, delete my account"}
              </AlertDialogAction>
            </AlertDialogFooter>
          </AlertDialogContent>
        </AlertDialog>
      </div>
    </div>
  );
}
```

Note: The account settings forms (email change, password change, delete account) are fully scaffolded with validation and UI per D-06 and UI-SPEC. The actual Server Action implementations for these operations are deferred — the forms show "not yet available" toasts. This is appropriate because D-06 establishes the page structure and the core feature of this phase is the reminders toggle, not account management. Account operations will be wired in Phase 7 (polish).

**3. Create src/app/(main)/preferences/page.tsx:**

Server Component that fetches user data and renders PreferencesForm:

```typescript
import { auth } from "../../../../auth";
import { db } from "@/lib/db";
import { redirect } from "next/navigation";
import { PreferencesForm } from "@/components/preferences/preferences-form";

export default async function PreferencesPage() {
  const session = await auth();
  if (!session?.user?.id) {
    redirect("/login");
  }

  const user = await db.user.findUnique({
    where: { id: session.user.id },
    select: { email: true, remindersEnabled: true },
  });

  if (!user) {
    redirect("/login");
  }

  return (
    <PreferencesForm
      initialRemindersEnabled={user.remindersEnabled}
      userEmail={user.email}
      isDemo={session.user.isDemo ?? false}
    />
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(main)/preferences/page.tsx contains `export default async function PreferencesPage(`
    - src/app/(main)/preferences/page.tsx contains `PreferencesForm` import
    - src/app/(main)/preferences/page.tsx contains `remindersEnabled` in select
    - src/components/preferences/preferences-form.tsx contains `export function PreferencesForm(`
    - src/components/preferences/preferences-form.tsx contains `"use client"`
    - src/components/preferences/preferences-form.tsx contains `In-app reminders`
    - src/components/preferences/preferences-form.tsx contains `When on, plants needing water appear in your notification center.`
    - src/components/preferences/preferences-form.tsx contains `toggleGlobalReminders` import
    - src/components/preferences/preferences-form.tsx contains `Preferences` as h1 text
    - src/components/preferences/preferences-form.tsx contains `Notifications` card heading
    - src/components/preferences/preferences-form.tsx contains `Account` card heading
    - src/components/preferences/account-settings.tsx contains `export function AccountSettings(`
    - src/components/preferences/account-settings.tsx contains `Delete your account?`
    - src/components/preferences/account-settings.tsx contains `Keep my account`
    - src/components/preferences/account-settings.tsx contains `Yes, delete my account`
    - src/components/preferences/account-settings.tsx contains `Update email`
    - src/components/preferences/account-settings.tsx contains `Update password`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Preferences page at /preferences with global reminders Switch toggle (per D-05), Notifications card heading (per UI-SPEC), and Account section with change email, change password, and delete account forms (per D-06). Optimistic toggle behavior with error revert. Account operations scaffolded with form validation, deferred to Phase 7 for full wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client components -> Server Actions | All toggle/snooze actions validate input and check ownership server-side |
| Nav layout -> reminder queries | Badge count computed server-side; client receives only the count number |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | SnoozePills | mitigate | Server Action validates plantId ownership and days range via Zod schema (1-365) |
| T-06-11 | Tampering | PlantReminderToggle | mitigate | Server Action validates plantId ownership before updating Reminder record |
| T-06-12 | Tampering | PreferencesForm global toggle | mitigate | Server Action only updates the authenticated user's remindersEnabled field |
| T-06-13 | Information Disclosure | NotificationBell dropdown | accept | Only shows plants owned by the authenticated user; no cross-user data leakage possible |
| T-06-14 | Denial of Service | Account delete | mitigate | AlertDialog confirmation required; demo users blocked from action |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npx vitest run --reporter=verbose` passes (no regressions)
3. NotificationBell renders in nav with correct aria-labels
4. SnoozePills render on overdue/due-today plants
5. Preferences page loads at /preferences
</verification>

<success_criteria>
- Bell icon shows badge count in nav, dropdown lists overdue/due-today plants
- Clicking dropdown item navigates to plant detail
- Snooze pills appear on overdue/due-today plant detail pages with 1d/2d/1w/Custom
- Per-plant reminder toggle shows on plant detail with disabled state when global off
- /preferences page shows global toggle and account settings
- Preferences link appears in nav
- All components handle demo mode (disabled state)
</success_criteria>

<output>
After completion, create `.planning/phases/06-reminders-and-demo-mode/06-03-SUMMARY.md`
</output>

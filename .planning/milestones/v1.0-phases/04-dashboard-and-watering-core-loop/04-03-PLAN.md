---
phase: 04-dashboard-and-watering-core-loop
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/components/watering/log-watering-dialog.tsx
  - src/components/watering/watering-history.tsx
  - src/components/watering/watering-history-entry.tsx
  - src/app/(main)/plants/[id]/page.tsx
autonomous: true
requirements:
  - WATR-03
  - WATR-04
  - WATR-05
  - WATR-06

must_haves:
  truths:
    - "User can view chronological watering history for each plant on the plant detail page, newest first, 20 per page with Load more pagination"
    - "User can log a retroactive watering via calendar date picker that defaults to today with future dates disabled"
    - "User can edit a watering log via kebab menu; edit opens same dialog pre-filled with existing date and note"
    - "User can delete a watering log via kebab menu with confirmation dialog; deletion recalculates nextWateringAt"
    - "Duplicate detection blocks same-calendar-day logs with informational toast"
    - "Optional note field (280 chars) is available on each watering log"
  artifacts:
    - path: "src/components/watering/log-watering-dialog.tsx"
      provides: "Dual-mode log/edit dialog with date picker and note field"
      contains: "LogWateringDialog"
    - path: "src/components/watering/watering-history.tsx"
      provides: "Paginated watering history list with load-more"
      contains: "WateringHistory"
    - path: "src/components/watering/watering-history-entry.tsx"
      provides: "Single history entry with kebab menu for edit/delete"
      contains: "WateringHistoryEntry"
    - path: "src/app/(main)/plants/[id]/page.tsx"
      provides: "Plant detail page integrating watering history via Timeline"
      contains: "PlantDetail"
  key_links:
    - from: "src/app/(main)/plants/[id]/page.tsx"
      to: "src/features/notes/queries.ts"
      via: "getTimeline call for unified watering+notes timeline"
      pattern: "getTimeline"
    - from: "src/components/watering/watering-history-entry.tsx"
      to: "src/components/watering/log-watering-dialog.tsx"
      via: "LogWateringDialog in edit mode with editLog prop"
      pattern: "editLog"
    - from: "src/components/watering/watering-history-entry.tsx"
      to: "src/features/watering/actions.ts"
      via: "deleteWateringLog Server Action call"
      pattern: "deleteWateringLog"
    - from: "src/components/watering/log-watering-dialog.tsx"
      to: "src/features/watering/actions.ts"
      via: "logWatering and editWateringLog Server Action calls"
      pattern: "logWatering|editWateringLog"
---

<objective>
Audit and verify the plant detail watering experience: watering history list, log/edit/delete dialogs, retroactive logging, and pagination. Confirm the Timeline integration pattern is correct.

Purpose: The watering history UI was implemented in prior phases via the Timeline component. This plan verifies that WATR-03 through WATR-06 are correctly served through the plant detail page, and that the log/edit/delete flows work end-to-end via the existing components.

Output: Verified plant detail watering experience with confirmed history, pagination, log/edit/delete dialogs, and duplicate detection.
</objective>

<execution_context>
@c:/Dev/poc/plantz-gsd/.claude/get-shit-done/workflows/execute-plan.md
@c:/Dev/poc/plantz-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-and-watering-core-loop/04-CONTEXT.md
@.planning/phases/04-dashboard-and-watering-core-loop/04-RESEARCH.md
@.planning/phases/04-dashboard-and-watering-core-loop/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/watering/log-watering-dialog.tsx:
```typescript
interface LogWateringDialogProps {
  plantId: string;
  plantNickname: string;
  editLog?: { id: string; wateredAt: Date; note: string | null };
  open?: boolean;
  onOpenChange?: (open: boolean) => void;
  onLogged?: () => void;
  onEdited?: () => void;
}
// Dual-mode: isEditMode = !!editLog
// Uses ResponsiveDialog (Dialog on desktop, Drawer on mobile)
// Fields: Calendar date picker (future disabled), Input note (280 char max)
```

From src/components/watering/watering-history.tsx:
```typescript
interface WateringHistoryProps {
  plantId: string;
  plantNickname: string;
  initialLogs: WateringLog[];
  totalCount: number;
  onMutated?: () => void;
}
// Pagination: loads 20 at a time via loadMoreWateringHistory
// "Load more" button when logs.length < total
```

From src/components/watering/watering-history-entry.tsx:
```typescript
interface WateringHistoryEntryProps {
  log: WateringLog;
  plantId: string;
  plantNickname: string;
  onDeleted?: () => void;
  onEdited?: () => void;
}
// Kebab menu: DropdownMenu with "Edit" and "Delete" items
// Edit opens LogWateringDialog in controlled edit mode
// Delete opens AlertDialog confirmation
```

From src/app/(main)/plants/[id]/page.tsx:
```typescript
// Uses getTimeline(id, userId) which returns unified watering + notes entries
// Renders PlantDetail with timelineEntries and timelineTotal props
// PlantDetail internally renders Timeline component
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify plant detail watering history integration and Timeline pattern</name>
  <files>src/app/(main)/plants/[id]/page.tsx, src/components/watering/watering-history.tsx, src/components/watering/watering-history-entry.tsx</files>
  <read_first>
    - src/app/(main)/plants/[id]/page.tsx
    - src/components/watering/watering-history.tsx
    - src/components/watering/watering-history-entry.tsx
    - src/components/plants/plant-detail.tsx
    - .planning/phases/04-dashboard-and-watering-core-loop/04-CONTEXT.md
    - .planning/phases/04-dashboard-and-watering-core-loop/04-RESEARCH.md
  </read_first>
  <action>
Audit the plant detail watering integration. This is a verification task -- the code is pre-built. The research warns about Pitfall 4: plans should NOT create a standalone watering history -- it is served through the Timeline.

**WATR-04 (Chronological watering history):** Verify:
1. Plant detail page calls `getTimeline(id, session.user.id)` which returns unified entries
2. `PlantDetail` receives `timelineEntries` and `timelineTotal` props
3. Timeline renders watering entries via `WateringHistoryEntry` component
4. `WateringHistory` component has pagination with `loadMoreWateringHistory` action and "Load more" button
5. History is ordered newest first (`orderBy: { wateredAt: "desc" }` in `getWateringHistory`)
6. Default take is 20 entries per page (D-09)

**WATR-05 (Edit/delete):** Verify:
1. `WateringHistoryEntry` has `DropdownMenu` with kebab trigger (`MoreVertical` icon, `h-8 w-8`)
2. "Edit" menu item opens `LogWateringDialog` in controlled edit mode via `editOpen` state
3. "Delete" menu item opens `AlertDialog` confirmation (D-12 deviation: confirmation exists per Phase 7)
4. Delete confirmation: title "Delete watering log?", description mentions recalculating next watering date
5. Cancel button: "Keep log", action button: "Delete log" with `variant="destructive"`
6. `onDeleted` callback removes log from local state and decrements total
7. `onEdited` callback triggers parent refetch

**WATR-06 (Duplicate detection in dialog):** Verify:
1. `LogWateringDialog` calls `logWatering` and checks for `DUPLICATE` error
2. On DUPLICATE: shows toast `"Already logged! Edit from history if needed."`
3. Does NOT close dialog on DUPLICATE (user can change date and retry)

**D-09 through D-12 (Watering History UX):** Verify:
1. Chronological list, newest first (D-09)
2. 20 entries per page with "Load more" button (D-09)
3. Calendar date picker defaults to today, future dates disabled (D-10)
4. Optional note field with 280 char max and placeholder text (D-11)
5. Kebab menu with edit/delete (D-12)

If ANY integration is missing, document the gap. If all integrations are correct, confirm.

No code changes expected unless a gap is found.
  </action>
  <acceptance_criteria>
    - plants/[id]/page.tsx calls `getTimeline(id, session.user.id)` and passes timelineEntries to PlantDetail
    - watering-history.tsx loads 20 entries at a time via `loadMoreWateringHistory(plantId, logs.length)`
    - watering-history.tsx shows "Load more" button when `logs.length < total`
    - watering-history.tsx renders `WateringHistoryEntry` for each log with `key={log.id}`
    - watering-history-entry.tsx has DropdownMenu with MoreVertical trigger button
    - watering-history-entry.tsx has "Edit" DropdownMenuItem that sets `editOpen` to true
    - watering-history-entry.tsx has "Delete" DropdownMenuItem with `text-destructive` class
    - watering-history-entry.tsx AlertDialog has title "Delete watering log?"
    - watering-history-entry.tsx AlertDialogCancel has text "Keep log"
    - watering-history-entry.tsx AlertDialogAction has text "Delete log" with variant="destructive"
    - watering-history-entry.tsx calls deleteWateringLog and shows toast "Watering log deleted."
    - log-watering-dialog.tsx handles DUPLICATE error with toast "Already logged! Edit from history if needed."
    - log-watering-dialog.tsx Calendar has `disabled={(date: Date) => date > new Date()}`
    - log-watering-dialog.tsx Input has `maxLength={280}` and placeholder containing "Optional note"
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/watering.test.ts</automated>
  </verify>
  <done>All plant detail watering requirements (WATR-03, WATR-04, WATR-05, WATR-06) verified present in existing code. Timeline integration pattern confirmed correct -- no standalone watering history needed. Log/edit/delete flows fully wired.</done>
</task>

<task type="auto">
  <name>Task 2: Verify LogWateringDialog dual-mode and D-10/D-11 design decisions</name>
  <files>src/components/watering/log-watering-dialog.tsx</files>
  <read_first>
    - src/components/watering/log-watering-dialog.tsx
    - .planning/phases/04-dashboard-and-watering-core-loop/04-UI-SPEC.md
    - .planning/phases/04-dashboard-and-watering-core-loop/04-CONTEXT.md
  </read_first>
  <action>
Verify the LogWateringDialog component against D-10, D-11, and the UI-SPEC copywriting contract:

**Dual-mode operation:**
1. Log mode (`!editLog`): Title "Log watering", cancel "Don't log", submit "Log watering"
2. Edit mode (`editLog`): Title "Edit watering log", cancel "Discard changes", submit "Save changes"
3. Controlled mode (edit from kebab): `open` + `onOpenChange` props
4. Uncontrolled mode (standalone trigger): Internal `internalOpen` state with trigger button "Log watering"

**D-10 (Retroactive logging):** Verify:
1. `Calendar` with `mode="single"` and `selected={field.value}`
2. `disabled={(date: Date) => date > new Date()}` -- future dates blocked
3. `weekStartsOn={1}` (Monday)
4. Default value for new log: `new Date()` (today)
5. Default value for edit: `editLog.wateredAt`

**D-11 (Optional note):** Verify:
1. `Input` with `maxLength={280}`
2. `placeholder="Optional note (e.g. used filtered water)"`
3. Label: `Note (optional)` with "(optional)" in muted text

**Copywriting contract verification (from UI-SPEC):**
1. Success toast (log mode): `"{plantNickname} watered on {Month Day}. Next: {Month Day}"`
2. Success toast (edit mode): `"Watering log updated."`
3. Duplicate toast: `"Already logged! Edit from history if needed."`
4. Error toast: `"Couldn't log watering. Try again."` (or server error message)
5. Dialog max width: `sm:max-w-[28rem]`
6. Primary button: `bg-accent text-accent-foreground hover:bg-accent/90`

**ResponsiveDialog integration:** Verify imports from `@/components/shared/responsive-dialog` (Dialog on desktop, Drawer on mobile).

No code changes expected unless a gap is found.
  </action>
  <acceptance_criteria>
    - log-watering-dialog.tsx uses ResponsiveDialog imports from `@/components/shared/responsive-dialog`
    - log-watering-dialog.tsx DialogContent has `sm:max-w-[28rem]`
    - log-watering-dialog.tsx log mode title is "Log watering"
    - log-watering-dialog.tsx edit mode title is "Edit watering log"
    - log-watering-dialog.tsx log mode cancel is "Don't log"
    - log-watering-dialog.tsx edit mode cancel is "Discard changes"
    - log-watering-dialog.tsx log mode submit is "Log watering"
    - log-watering-dialog.tsx edit mode submit is "Save changes"
    - log-watering-dialog.tsx Calendar has `weekStartsOn={1}`
    - log-watering-dialog.tsx Input has `maxLength={280}`
    - log-watering-dialog.tsx placeholder contains "Optional note (e.g. used filtered water)"
    - log-watering-dialog.tsx submit button has `bg-accent text-accent-foreground hover:bg-accent/90`
    - log-watering-dialog.tsx success toast contains "watered on" and "Next:"
    - log-watering-dialog.tsx edit success toast is "Watering log updated."
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/watering.test.ts</automated>
  </verify>
  <done>LogWateringDialog dual-mode operation, D-10 retroactive logging, D-11 note field, and UI-SPEC copywriting contract all verified. ResponsiveDialog integration confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client dialog form -> Server Action | Untrusted date and note input from LogWateringDialog |
| Client delete click -> Server Action | logId from client used to identify record to delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | LogWateringDialog wateredAt | mitigate | Zod schema refine blocks future dates; `z.coerce.date()` handles string-to-Date coercion |
| T-04-12 | Tampering | LogWateringDialog note | mitigate | Zod schema `.max(280)` enforces length; React escapes strings in JSX (no XSS via dangerouslySetInnerHTML) |
| T-04-13 | Elevation of privilege | deleteWateringLog | mitigate | Ownership check via `plant: { userId: session.user.id }` in findFirst before delete |
| T-04-14 | Elevation of privilege | editWateringLog | mitigate | Same ownership check as T-04-13 through plant relation |
</threat_model>

<verification>
All tests pass with `npx vitest run tests/watering.test.ts`. Plant detail integration verified by code audit. Manual verification needed for: calendar interaction, dialog animations, kebab menu UX (per 04-VALIDATION.md manual-only items).
</verification>

<success_criteria>
- All watering history requirements (WATR-03, WATR-04, WATR-05, WATR-06) verified present in code
- LogWateringDialog dual-mode, retroactive logging, note field, and copywriting all verified
- Timeline integration pattern confirmed correct (no standalone history list)
- No regressions in test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-and-watering-core-loop/04-03-SUMMARY.md`
</output>

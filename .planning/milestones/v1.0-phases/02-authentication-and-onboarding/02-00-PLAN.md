---
phase: 02-authentication-and-onboarding
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/auth.test.ts
  - tests/register-form.test.tsx
  - tests/onboarding-banner.test.tsx
  - e2e/auth.spec.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "Test stubs exist for all phase 2 behaviors before implementation begins"
    - "Vitest can discover and run the new test files without errors"
    - "Playwright can discover the E2E spec file without errors"
  artifacts:
    - path: "tests/auth.test.ts"
      provides: "Auth unit test stubs covering registration, login, session callbacks"
      contains: "describe"
    - path: "tests/register-form.test.tsx"
      provides: "Register form component test stubs"
      contains: "describe"
    - path: "tests/onboarding-banner.test.tsx"
      provides: "Onboarding banner component test stubs"
      contains: "describe"
    - path: "e2e/auth.spec.ts"
      provides: "E2E auth flow spec stubs"
      contains: "test"
  key_links: []
---

<objective>
Create test stubs and fixtures for all Phase 2 behaviors BEFORE implementation plans run. This is the Wave 0 Nyquist compliance plan: every AUTH requirement gets a failing test file that subsequent plans will make pass.

Purpose: Establishes the behavioral verification contract upfront. Implementation plans (02-01, 02-02, 02-03) can then use these test files as automated verification instead of relying solely on `npx tsc --noEmit`.

Output: 4 test files with descriptive stubs (expected to fail or be skipped until implementation completes).
</objective>

<execution_context>
@c:/Dev/poc/plantz-gsd/.claude/get-shit-done/workflows/execute-plan.md
@c:/Dev/poc/plantz-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication-and-onboarding/02-CONTEXT.md
@.planning/phases/02-authentication-and-onboarding/02-VALIDATION.md

<interfaces>
<!-- Existing test infrastructure -->

From vitest.config.mts:
```typescript
export default defineConfig({
  plugins: [tsconfigPaths(), react()],
  test: {
    environment: "jsdom",
    include: ["tests/**/*.{test,spec}.{ts,tsx}"],
  },
});
```

From playwright.config.ts:
```typescript
export default defineConfig({
  testDir: "./e2e",
  // ...
  webServer: {
    command: "npm run dev",
    url: "http://localhost:3000",
    reuseExistingServer: !process.env.CI,
  },
});
```

From tests/auth.test.ts (existing Phase 1 auth tests — will be EXTENDED, not replaced):
```typescript
describe("auth configuration", () => {
  test("auth.config.ts is edge-safe (no Node.js API imports)", async () => { ... });
  test("auth.ts has Credentials provider with Zod validation", async () => { ... });
  test("proxy.ts exports auth as proxy", async () => { ... });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend auth.test.ts with Phase 2 behavior stubs + create component test stubs</name>
  <files>tests/auth.test.ts, tests/register-form.test.tsx, tests/onboarding-banner.test.tsx</files>
  <read_first>
    - tests/auth.test.ts (existing Phase 1 tests — MUST preserve all existing tests, only ADD new describe blocks)
    - vitest.config.mts (confirm test file pattern includes tests/**/*.{test,spec}.{ts,tsx})
  </read_first>
  <action>
**1. Extend tests/auth.test.ts — Add Phase 2 behavior stubs:**

PRESERVE all 3 existing tests in the "auth configuration" describe block. Add NEW describe blocks after the existing one for Phase 2 behaviors:

```typescript
// --- Phase 2 behavior stubs (Wave 0) ---
// These tests verify behaviors added by Plan 02-01.
// They will fail until implementation is complete.

describe("auth session callbacks (Phase 2)", () => {
  test.todo("jwt callback stores user.id in token when user object is present");
  test.todo("session callback copies token.id to session.user.id");
});

describe("registration action (Phase 2)", () => {
  test.todo("registerUser creates a user with bcrypt-hashed password");
  test.todo("registerUser returns error for duplicate email");
  test.todo("registerUser returns error for invalid input (Zod validation)");
  test.todo("registerUser calls signIn after successful creation");
});

describe("onboarding action (Phase 2)", () => {
  test.todo("completeOnboarding updates user onboardingCompleted to true");
  test.todo("completeOnboarding validates plantCountRange against enum");
  test.todo("completeOnboarding returns error for invalid range");
});

describe("zod schemas (Phase 2)", () => {
  test.todo("loginSchema accepts valid email and password");
  test.todo("loginSchema rejects invalid email");
  test.todo("registerSchema rejects password shorter than 6 characters");
  test.todo("registerSchema rejects mismatched passwords");
  test.todo("onboardingSchema accepts valid plant count range");
  test.todo("onboardingSchema rejects invalid plant count range");
});
```

Using `test.todo()` is the correct Vitest pattern for stub tests — they are reported as "todo" in the test output without failing the suite. This lets the test suite stay green during Wave 0 while clearly documenting what behaviors will be verified.

**2. Create tests/register-form.test.tsx — Register form component test stubs:**

```typescript
import { describe, test } from "vitest";

describe("RegisterForm component (Phase 2)", () => {
  test.todo("renders email, password, and confirm password fields");
  test.todo("shows inline error for invalid email on submit");
  test.todo("shows inline error for password shorter than 6 characters");
  test.todo("shows inline error when passwords do not match");
  test.todo("disables all fields and shows loading spinner during submission");
  test.todo("displays cross-link to login page");
  test.todo("calls registerUser server action with form values on valid submit");
  test.todo("shows toast notification on server error response");
});
```

**3. Create tests/onboarding-banner.test.tsx — Onboarding banner component test stubs:**

```typescript
import { describe, test } from "vitest";

describe("OnboardingBanner component (Phase 2)", () => {
  test.todo("renders welcome message and plant count range buttons");
  test.todo("renders all 4 plant range options: 1-5, 6-15, 16-30, 30+");
  test.todo("highlights selected range button with accent styling");
  test.todo("calls completeOnboarding server action when range is selected");
  test.todo("shows confirmation text after successful completion");
  test.todo("collapses banner with animation after completion");
  test.todo("dismiss button hides the banner without calling server action");
  test.todo("dismiss button has accessible aria-label");
});
```

These component test files use `.tsx` extension because they will eventually render React components. For now they only contain `test.todo()` stubs which need no JSX.
  </action>
  <verify>
    <automated>cd C:/Dev/poc/plantz-gsd && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>tests/auth.test.ts has new Phase 2 describe blocks with todo stubs alongside existing passing Phase 1 tests. tests/register-form.test.tsx and tests/onboarding-banner.test.tsx exist with todo stubs. `npx vitest run` passes (existing tests green, new stubs show as todo). Test count increases.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E auth spec stubs</name>
  <files>e2e/auth.spec.ts</files>
  <read_first>
    - e2e/smoke.spec.ts (existing E2E test — understand the Playwright pattern used)
    - playwright.config.ts (confirm testDir is ./e2e)
  </read_first>
  <action>
**Create e2e/auth.spec.ts — E2E auth flow spec stubs:**

```typescript
import { test } from "@playwright/test";

test.describe("authentication flow (Phase 2)", () => {
  test.fixme("unauthenticated user visiting / is redirected to /login", async ({ page }) => {
    // AUTH-05: Route protection
  });

  test.fixme("user can register with email and password and lands on /dashboard", async ({ page }) => {
    // AUTH-01: Registration + D-03: auto-login redirect
  });

  test.fixme("user can log in with existing credentials", async ({ page }) => {
    // AUTH-02: Login with persistent session
  });

  test.fixme("user can log out and is redirected to /login", async ({ page }) => {
    // AUTH-03: Logout
  });

  test.fixme("new user sees onboarding banner on dashboard", async ({ page }) => {
    // AUTH-04: Onboarding after first login
  });

  test.fixme("user can complete onboarding by selecting plant count range", async ({ page }) => {
    // AUTH-04: Onboarding completion + D-07: range buttons
  });

  test.fixme("login page shows error toast for wrong credentials", async ({ page }) => {
    // AUTH-02: Error handling
  });

  test.fixme("register page shows inline validation errors", async ({ page }) => {
    // AUTH-01: Client-side validation + D-02
  });
});
```

Using `test.fixme()` is the Playwright equivalent of Vitest's `test.todo()` — it marks tests as known-incomplete without failing the suite. Each stub includes a comment referencing the requirement ID it will verify.

Note: E2E tests require the full app running. They will be fleshed out as Plans 02-01, 02-02, and 02-03 complete implementation. The stubs document the contract of what must be verifiable end-to-end.
  </action>
  <verify>
    <automated>cd C:/Dev/poc/plantz-gsd && npx playwright test --list 2>&1 | tail -15</automated>
  </verify>
  <done>e2e/auth.spec.ts exists with fixme stubs for all auth flow scenarios. `npx playwright test --list` shows the new specs alongside the existing smoke.spec.ts. No test failures introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan creates test stubs only, no production code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | Test-only plan; no production code, no trust boundaries |
</threat_model>

<verification>
1. `npx vitest run --reporter=verbose` passes with existing Phase 1 tests green and new Phase 2 stubs showing as todo
2. `npx playwright test --list` shows e2e/auth.spec.ts alongside e2e/smoke.spec.ts
3. tests/auth.test.ts preserves all 3 existing Phase 1 tests
4. tests/register-form.test.tsx exists with todo stubs
5. tests/onboarding-banner.test.tsx exists with todo stubs
</verification>

<success_criteria>
- All 4 test files exist with descriptive behavior stubs
- Existing Phase 1 tests in tests/auth.test.ts are preserved and still pass
- Vitest suite runs green (todo tests reported but not failing)
- Playwright lists the new E2E spec without errors
- Wave 0 is complete — implementation plans can reference these test files
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-and-onboarding/02-00-SUMMARY.md`
</output>
